query FilterCharactersByName($character_name: String!) {
    characters(filter: {name: $character_name} ) {
        results {
            id
            name
            status
            image
        }
    }
}